# Backend API URL
# Dev: /api (Vite proxy forwards to localhost:3000)
# Production: set to deployed backend URL
VITE_API_URL=/api

# Telegram bot username (no @) used to build share / deep links.
VITE_BOT_USERNAME=OroPredictBot
